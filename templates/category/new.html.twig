{% extends 'base.html.twig' %}

{% block title %}Optime - New Category{% endblock %}

{% block body %}
    <div class="row">
    <div class="col"><h3>Create new Category</h3></div>
    <div class="col text-right"><a href="{{ path('product_index') }}" class="btn btn-primary">Products</a></div>
    </div>

    {{ include('category/_form.html.twig') }}

    <a href="{{ path('category_index') }}" class="btn btn-primary">back to list</a>
{% endblock %}
